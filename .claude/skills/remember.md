Review everything that happened in this session and save what's worth keeping to the project memory system at `C:\Users\nickd\.claude\projects\c--Users-nickd-Documents-Projects-pan-for-gold\memory\`.

For each memory type, ask: "would this help future-me work better with this user?"

**What to capture:**

- **feedback**: Things the user corrected or validated — their working style, what pisses them off, what they approved. Examples from this kind of project: "don't add bank colors inside water tiles", "water tiles fill the full rect, no masking", "don't add things that weren't asked for". Include *why* so edge cases can be judged later.

- **user**: Anything learned about who they are, their skill level, how they prefer to communicate, their aesthetic sensibilities, domain knowledge.

- **project**: Decisions made, current state of the build phase, architectural choices. Use absolute dates, not relative ones.

- **reference**: External resources, tools, or locations referenced during the session.

**Process:**
1. Read `MEMORY.md` to see what already exists
2. For each existing memory that was touched or contradicted this session — update it
3. For genuinely new insights — create a new file, add a pointer to `MEMORY.md`
4. Don't save ephemeral task details, code patterns, or anything already obvious from the codebase

Be selective. One sharp memory beats five vague ones.
