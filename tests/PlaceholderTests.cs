namespace pan_for_gold.Tests;

// Pure C# logic tests live here.
// Note: game classes that extend Godot.Node cannot be instantiated here —
// extract logic into plain C# classes and reference them from both the
// game project and this test project.
public class PlaceholderTests
{
    [Fact]
    public void Placeholder_AlwaysPasses()
    {
        Assert.True(true);
    }
}